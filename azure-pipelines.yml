trigger:
  branches:
    include:
      - main
      - develop
  paths:
    include:
      - '**'

resources:
  repositories:
    - repository: SocketService
      type: github
      name: TheHiddenCargo/SocketService
      endpoint: github-connection

extends:
  template: azure-pipelines-template.yml@socket
  parameters:
    projectName: 'bidManager'
    servicePort: '8083'
    environment: 'production'
    additionalSteps:
      - script: |
          echo "Running Bids-specific build steps"
        displayName: 'Bids Manager'